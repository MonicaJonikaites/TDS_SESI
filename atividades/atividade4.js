let lapis = 5;
let caderno = 20;
let borracha = 4;

let valorTotal = lapis + caderno + borracha;
    let mensagem = valorTotal > 100 ? - (valorTotal* 0.10): valorTotal
    console.log("O valor do desconto é: " + mensagem);